{
  "name": "bismi-pg-app",
  "version": "3.0.0",
  "private": true,
  "description": "Bismi PG Management App - Full Stack",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && node server.js",
    "install-all": "cd frontend && npm install && cd ../backend && npm install"
  }
}
